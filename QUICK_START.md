# Быстрый старт - XAMPP + phpMyAdmin

## ✅ XAMPP - это уже всё, что нужно!

**XAMPP включает:**
- ✅ MySQL (база данных)
- ✅ phpMyAdmin (управление БД через браузер)
- ✅ Apache (веб-сервер)
- ✅ PHP

**Вам НЕ нужно устанавливать MySQL отдельно!**

---

## 📋 Пошаговая установка (5 минут)

### Шаг 1: Установите XAMPP
1. Скачайте с https://www.apachefriends.org/
2. Установите (обычно в `C:\xampp\`)

### Шаг 2: Запустите серверы
1. Откройте **XAMPP Control Panel**
2. Нажмите **Start** для **Apache**
3. Нажмите **Start** для **MySQL**

### Шаг 3: Создайте базу данных
1. Нажмите кнопку **Admin** рядом с MySQL (откроется phpMyAdmin)
2. Или откройте: `http://localhost/phpmyadmin`
3. Нажмите вкладку **SQL** (сверху)
4. Откройте файл `backend/database_schema.sql`
5. Скопируйте весь текст и вставьте в окно SQL
6. Нажмите **Выполнить** (Go)

### Шаг 4: Разместите проект
1. Скопируйте папку `korochnik` в `C:\xampp\htdocs\`
2. Путь должен быть: `C:\xampp\htdocs\korochnik\`

### Шаг 5: Настройте путь к API
1. Откройте файл `frontend/app.js`
2. Найдите строку 24:
   ```javascript
   const API_BASE = 'http://localhost/korochnik/backend/api';
   ```
3. Если проект в `htdocs/korochnik/` - **ничего менять не нужно!**
4. Если проект в другом месте - измените путь (см. примеры ниже)

### Шаг 6: Откройте сайт
Откройте в браузере: `http://localhost/korochnik/frontend/index.html`

---

## 🔧 Настройка пути к API

### Где находится настройка?

**Файл:** `frontend/app.js`  
**Строка:** 24

```javascript
const API_BASE = 'http://localhost/korochnik/backend/api';
```

### Что это значит?

Это адрес, по которому JavaScript обращается к PHP-файлам на сервере.

**Пример работы:**
- Вы открываете: `http://localhost/korochnik/frontend/index.html`
- JavaScript отправляет запрос: `http://localhost/korochnik/backend/api/auth.php`
- PHP обрабатывает запрос и возвращает ответ

### Как изменить путь?

#### ✅ Стандартный вариант (XAMPP в htdocs)
Если структура:
```
C:\xampp\htdocs\korochnik\
```

То путь (уже настроен):
```javascript
const API_BASE = 'http://localhost/korochnik/backend/api';
```

#### Вариант 2: Проект в корне htdocs
Если структура:
```
C:\xampp\htdocs\
  ├── frontend\
  └── backend\
```

То путь:
```javascript
const API_BASE = 'http://localhost/backend/api';
```

#### Вариант 3: Другой порт
Если Apache на порту 8080:
```javascript
const API_BASE = 'http://localhost:8080/korochnik/backend/api';
```

#### Вариант 4: Виртуальный хост
Если настроен домен `korochnik.local`:
```javascript
const API_BASE = 'http://korochnik.local/backend/api';
```

### Автоматическое определение пути

Если хотите, чтобы путь определялся автоматически, раскомментируйте код в `app.js` (строки 15-22):

```javascript
// Раскомментируйте эти строки:
const getBasePath = () => {
    const protocol = window.location.protocol;
    const host = window.location.host;
    const pathname = window.location.pathname;
    const basePath = pathname.split('/frontend')[0];
    return `${protocol}//${host}${basePath}/backend/api`;
};
const API_BASE = getBasePath();

// И закомментируйте старую строку:
// const API_BASE = 'http://localhost/korochnik/backend/api';
```

---

## ✅ Проверка настройки

### 1. Проверьте базу данных
1. Откройте phpMyAdmin: `http://localhost/phpmyadmin`
2. В левом меню должна быть база `korochnik`
3. В ней должны быть таблицы: `users`, `courses`, `applications`

### 2. Проверьте путь к API
1. Откройте сайт: `http://localhost/korochnik/frontend/index.html`
2. Нажмите **F12** (консоль разработчика)
3. Перейдите на вкладку **Network** (Сеть)
4. Попробуйте зарегистрироваться
5. Должен быть запрос к `auth.php` со статусом **200** (OK)

### 3. Проверьте работу
1. Зарегистрируйте нового пользователя
2. Войдите в систему
3. Подайте заявку на курс
4. Войдите как администратор (Admin / KorokNET)
5. Измените статус заявки

---

## 🐛 Решение проблем

### Ошибка: "Ошибка соединения с сервером"

**Причина:** Неправильный путь к API или Apache не запущен

**Решение:**
1. Проверьте, что Apache запущен в XAMPP
2. Проверьте путь в `frontend/app.js`
3. Откройте консоль браузера (F12) → вкладка Console

### Ошибка: "404 Not Found"

**Причина:** Неправильный путь к API

**Решение:**
1. Проверьте структуру папок
2. Попробуйте открыть напрямую: `http://localhost/korochnik/backend/api/courses.php`
3. Если открывается - путь правильный
4. Если 404 - измените путь в `app.js`

### Ошибка: "Connection error" в базе данных

**Причина:** MySQL не запущен или неправильные настройки

**Решение:**
1. Проверьте, что MySQL запущен в XAMPP
2. Проверьте `backend/config/database.php`:
   ```php
   private $host = "localhost";
   private $db_name = "korochnik";
   private $username = "root";
   private $password = ""; // Пустой для XAMPP
   ```
3. Проверьте, что база `korochnik` создана в phpMyAdmin

---

## 📝 Краткая шпаргалка

```
1. XAMPP → Start Apache и MySQL
2. phpMyAdmin → Импортировать database_schema.sql
3. Проект → C:\xampp\htdocs\korochnik\
4. Путь API → frontend/app.js (строка 24)
5. Открыть → http://localhost/korochnik/frontend/index.html
```

---

## 🎯 Готово!

Если всё работает - можете начинать использовать систему!

**Учетные данные администратора:**
- Логин: `Admin`
- Пароль: `KorokNET`

