# Руководство по установке и настройке

## 1. XAMPP - это уже то, что нужно! ✅

**XAMPP включает в себя:**
- ✅ **MySQL** - база данных (уже встроена)
- ✅ **phpMyAdmin** - веб-интерфейс для управления MySQL (уже встроен)
- ✅ **Apache** - веб-сервер
- ✅ **PHP** - язык программирования

**Вам НЕ нужно устанавливать MySQL отдельно!** XAMPP уже содержит всё необходимое.

### Шаги установки с XAMPP:

1. **Скачайте и установите XAMPP** с официального сайта: https://www.apachefriends.org/

2. **Запустите XAMPP Control Panel**

3. **Запустите модули:**
   - Нажмите "Start" для **Apache**
   - Нажмите "Start" для **MySQL**

4. **Откройте phpMyAdmin:**
   - Нажмите кнопку "Admin" рядом с MySQL
   - Или откройте в браузере: `http://localhost/phpmyadmin`

5. **Создайте базу данных:**
   - В phpMyAdmin нажмите "SQL" (вкладка сверху)
   - Скопируйте содержимое файла `backend/database_schema.sql`
   - Вставьте в окно SQL
   - Нажмите "Выполнить" (Go)

   ИЛИ импортируйте файл:
   - Нажмите "Импорт" (Import) в меню
   - Выберите файл `backend/database_schema.sql`
   - Нажмите "Выполнить"

6. **Поместите проект в папку htdocs:**
   - Скопируйте папку `korochnik` в `C:\xampp\htdocs\`
   - Путь должен быть: `C:\xampp\htdocs\korochnik\`

7. **Откройте сайт:**
   - В браузере: `http://localhost/korochnik/frontend/index.html`

## 2. Настройка путей к API

### Что такое путь к API?

Путь к API - это адрес, по которому JavaScript-код обращается к PHP-файлам на сервере.

**Текущий путь в коде:**
```javascript
const API_BASE = 'http://localhost/korochnik/backend/api';
```

Это означает, что когда вы открываете сайт по адресу:
- `http://localhost/korochnik/frontend/index.html`

JavaScript будет обращаться к API по адресу:
- `http://localhost/korochnik/backend/api/auth.php`
- `http://localhost/korochnik/backend/api/applications.php`
- и т.д.

### Как настроить путь под ваш сервер?

#### Вариант 1: Если проект в папке htdocs (стандартный XAMPP)

Если структура такая:
```
C:\xampp\htdocs\korochnik\
```

То путь должен быть:
```javascript
const API_BASE = 'http://localhost/korochnik/backend/api';
```

#### Вариант 2: Если проект в корне htdocs

Если структура такая:
```
C:\xampp\htdocs\
  ├── index.html
  ├── frontend\
  └── backend\
```

То путь должен быть:
```javascript
const API_BASE = 'http://localhost/backend/api';
```

#### Вариант 3: Если используется виртуальный хост

Если вы настроили виртуальный хост (например, `korochnik.local`), то:
```javascript
const API_BASE = 'http://korochnik.local/backend/api';
```

#### Вариант 4: Если проект на другом порту

Если Apache работает на другом порту (например, 8080):
```javascript
const API_BASE = 'http://localhost:8080/korochnik/backend/api';
```

### Где изменить путь?

Нужно изменить в **одном месте** - в файле `frontend/app.js`:

**Текущий код (строка 2):**
```javascript
const API_BASE = 'http://localhost/korochnik/backend/api';
```

**Измените на ваш путь:**
```javascript
const API_BASE = 'http://ваш-адрес/ваш-путь/backend/api';
```

### Автоматическое определение пути (рекомендуется)

Можно сделать так, чтобы путь определялся автоматически:

```javascript
// Автоматическое определение базового пути
const getBasePath = () => {
    const protocol = window.location.protocol; // http: или https:
    const host = window.location.host; // localhost или ваш домен
    const pathname = window.location.pathname; // /korochnik/frontend/index.html
    
    // Убираем /frontend/index.html и добавляем /backend/api
    const basePath = pathname.split('/frontend')[0];
    return `${protocol}//${host}${basePath}/backend/api`;
};

const API_BASE = getBasePath();
```

Этот код автоматически определит правильный путь независимо от того, где находится проект!

### Проверка правильности пути

1. Откройте сайт в браузере
2. Нажмите F12 (открыть консоль разработчика)
3. Перейдите на вкладку "Network" (Сеть)
4. Попробуйте зарегистрироваться или войти
5. Посмотрите, какие запросы отправляются

**Правильный запрос должен быть:**
- URL: `http://localhost/korochnik/backend/api/auth.php`
- Status: 200 (OK) или другой успешный код

**Неправильный запрос:**
- URL: `http://localhost/backend/api/auth.php` (404 Not Found)
- Это значит, путь указан неверно

### Примеры для разных ситуаций

#### Если проект в подпапке:
```
http://localhost/myprojects/korochnik/frontend/index.html
```
Путь к API:
```javascript
const API_BASE = 'http://localhost/myprojects/korochnik/backend/api';
```

#### Если используется OpenServer (альтернатива XAMPP):
```
http://korochnik/
```
Путь к API:
```javascript
const API_BASE = 'http://korochnik/backend/api';
```

#### Если проект на удаленном сервере:
```
https://mysite.com/korochnik/frontend/index.html
```
Путь к API:
```javascript
const API_BASE = 'https://mysite.com/korochnik/backend/api';
```

## 3. Быстрая проверка настройки

1. ✅ XAMPP запущен (Apache и MySQL)
2. ✅ База данных создана через phpMyAdmin
3. ✅ Проект в папке `htdocs/korochnik/`
4. ✅ Путь к API настроен в `frontend/app.js`
5. ✅ Откройте `http://localhost/korochnik/frontend/index.html`

Если всё работает - готово! 🎉

## 4. Решение проблем

### Проблема: "Ошибка соединения с сервером"

**Решение:**
1. Проверьте, что Apache запущен в XAMPP
2. Проверьте путь к API в `frontend/app.js`
3. Откройте консоль браузера (F12) и посмотрите ошибки

### Проблема: "404 Not Found" при запросах к API

**Решение:**
1. Проверьте, что файлы PHP находятся в `backend/api/`
2. Проверьте путь к API - он должен быть правильным
3. Попробуйте открыть напрямую: `http://localhost/korochnik/backend/api/courses.php`

### Проблема: База данных не подключается

**Решение:**
1. Проверьте, что MySQL запущен в XAMPP
2. Проверьте настройки в `backend/config/database.php`:
   ```php
   private $host = "localhost";
   private $db_name = "korochnik";
   private $username = "root";
   private $password = ""; // Пустой для XAMPP по умолчанию
   ```
3. Проверьте, что база данных `korochnik` создана в phpMyAdmin

## 5. Альтернативы XAMPP

Если XAMPP не подходит, можно использовать:

- **OpenServer** (для Windows) - аналогичен XAMPP, но на русском
- **WAMP** (для Windows) - Windows Apache MySQL PHP
- **LAMP** (для Linux) - Linux Apache MySQL PHP
- **MAMP** (для Mac) - Mac Apache MySQL PHP

Все они работают одинаково и включают MySQL + phpMyAdmin!

